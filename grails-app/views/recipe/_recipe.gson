import companion.Recipe

model{
    Recipe recipe
}

json g.render(recipe){
    recipeIngredients recipe.recipeIngredients*.properties*.subMap(['id', 'ingredient', 'amount', 'unit' ])
    categories recipe.categories*.properties*.subMap(['id', 'name'])
}